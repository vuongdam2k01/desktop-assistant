# Risks Ledger

Harvested 2026-09-12 by `specdocs:harvest` from `docs/raw-idea/prd-mvp.md` and the twenty spike reports under `spikes/`.
Status values: `open` (no mitigation in place), `partially mitigated`, `mitigated` (a harvested change specifies the mitigation), `resolved` (the cause was removed).

Amended 2026-09-12 by change `req-022-account-sync` (constitution 2.0.0): the product became account-owned with
server-side replication, which changed three existing rows and added nine new ones (RISK-062..RISK-070).

| ID | Risk | Source | Status | Mitigation |
| --- | --- | --- | --- | --- |
| RISK-001 | Some write operations have no static compensation formula, or the snapshot is too coarse to build one | `docs/raw-idea/prd-mvp.md#16-rui-ro-amp-phuong-an-giam-thieu` | mitigated | Measured per operation; four binding manifest conditions recorded in `req-003-notion-compensation`. Irreversible is an accepted outcome, flagged and surfaced in the undo preview. |
| RISK-002 | The rule elicitation conversation converges poorly, or the compiled rule blocks too much or too little | `docs/raw-idea/prd-mvp.md#16-rui-ro-amp-phuong-an-giam-thieu` | mitigated with condition | Strong model converges in 4.35 turns with zero silent downgrade; cheap model forbidden in this role. See `req-004-rule-elicitation`. |
| RISK-003 | Stable always-on-top rendering on both operating systems is harder than expected | `docs/raw-idea/prd-mvp.md#16-rui-ro-amp-phuong-an-giam-thieu` | partially mitigated | Verified on Windows at 60 fps with clean transparency (`req-005-electron-rive-pet-render`); macOS deferred and unverified. |
| RISK-004 | The agentic loop is not reliable enough on real work, so jobs go wrong and users lose trust early | `docs/raw-idea/prd-mvp.md#16-rui-ro-amp-phuong-an-giam-thieu` | mitigated | 85.0% final-state correctness against an 80% floor, with 95% self-verification. See `req-006-agent-loop`. |
| RISK-005 | Approval in on mode annoys users into disabling control entirely, then an incident costs double trust | `docs/raw-idea/prd-mvp.md#16-rui-ro-amp-phuong-an-giam-thieu` | open | Smart mode as default after the first week; fast approval directly on the pet card. Not yet measured with users. |
| RISK-006 | Model and job cost exceeds what users will accept | `docs/raw-idea/prd-mvp.md#16-rui-ro-amp-phuong-an-giam-thieu` | mitigated | Measured at about $0.167 per month with the role split; token usage and estimated cost shown per job. See `req-017-provider-matrix`. |
| RISK-007 | The pet's character irritates rather than endears | `docs/raw-idea/prd-mvp.md#16-rui-ro-amp-phuong-an-giam-thieu` | open | Persona specification reviewed early; talkativeness A/B tested in beta; a control to reduce notifications always available. |
| RISK-008 | Prompt injection through connector content or screenshots makes the agent act outside its instruction | `docs/raw-idea/prd-mvp.md#16-rui-ro-amp-phuong-an-giam-thieu` | mitigated | Zero leakage across 20 adversarial cases with the gate outside the model loop (`req-009-rule-ir-hardgate`), plus untrusted-data handling in the risk judge (`req-011-risk-judge`). |
| RISK-009 | Backend outage | `docs/raw-idea/prd-mvp.md#16-rui-ro-amp-phuong-an-giam-thieu` | partially mitigated | Running jobs and existing connections continue because the local store is the working copy and model calls go client-direct (`req-020-backend-slice`, `req-021-ask-user-offline`). Since `req-022-account-sync` an outage additionally stalls replication, so devices drift apart until it clears and a second device shows stale history. |
| RISK-010 | Server-side secret exposure, now including the replication keys that protect every account's work content | `docs/raw-idea/prd-mvp.md#16-rui-ro-amp-phuong-an-giam-thieu` | open | Secret manager with periodic rotation, least privilege, access audit log, and secret scanning in continuous integration. **Severity raised by `req-022-account-sync`**: the blast radius now includes the replication keys, so a server-side compromise exposes user work content rather than only service credentials. Key access confined to the replication path and audited is the control, and it is the entire privacy boundary. |
| RISK-011 | Beta users are surprised by their own provider costs; the team lacks cost-per-job data | `docs/raw-idea/prd-mvp.md#16-rui-ro-amp-phuong-an-giam-thieu` | mitigated | Onboarding states the bring-your-own-provider model; token usage shown in job detail. See `req-017-provider-matrix`. |
| RISK-012 | Bring-your-own OAuth client is the only email path, and every user must complete a ten-step console setup with seven-day re-consent | `docs/raw-idea/prd-mvp.md#16-rui-ro-amp-phuong-an-giam-thieu` | open | Accepted deliberately for the single-user phase. Exit condition: complete provider verification before onboarding a second non-technical user. Route verified working in `req-014-byo-oauth-google`. |
| RISK-013 | Violating the provider's limited-use policy by sending message content to a model, and by replicating content derived from it to our servers | `docs/raw-idea/prd-mvp.md#16-rui-ro-amp-phuong-an-giam-thieu` | open | Explicit disclosure at onboarding stating the model is one the user configured; no background indexing. **Sharpened by `req-022-account-sync`**: the backend no longer merely avoids receiving message content — replicated ledger records and transcripts containing that content now rest on our infrastructure encrypted at rest, which turns this from a disclosure question into a data-handling one the verification submission must address. |
| RISK-014 | Dependency on a young open-source agent framework: breaking changes or abandonment | `docs/raw-idea/prd-mvp.md#16-rui-ro-amp-phuong-an-giam-thieu` | partially mitigated | Versions pinned and lockfile fixed; the wrap and ledger layer sits outside the framework, so replacing it does not break the trust chain. See `req-007-pi-sdk-harness`. |
| RISK-015 | Verification bias when the designer is the only user: overfitting to personal habits and tolerating weak experience | `docs/raw-idea/prd-mvp.md#16-rui-ro-amp-phuong-an-giam-thieu` | open | Structured self-verification protocol; use the product through the official flows only; exit condition is two or three trusted people using it on their own data. |
| RISK-016 | Suspending a third-party input-method process stalls the operating system's low-level hook pipeline and makes the user's physical cursor stutter | `spikes/SP-0-gui-harness/REPORT.md#4-rui-ro-moi-phat-hien` | resolved | The technique was removed permanently from all tooling and is now a standing product prohibition. See `req-002-gui-spike-harness`. |
| RISK-017 | The agent cannot act on the operating system's secure desktop, so elevation prompts block automated sessions | `spikes/SP-0-gui-harness/REPORT.md#4-rui-ro-moi-phat-hien` | open | Any session needing a native toolchain or certificate install starts from an elevated terminal. |
| RISK-018 | Synthesised modifier-key pulses trigger menu accelerators in other applications, and forced cursor positioning disturbs the user | `spikes/SP-0-gui-harness/REPORT.md#4-rui-ro-moi-phat-hien` | resolved | Both removed; only plain foreground activation remains. |
| RISK-019 | Nothing prevented the product itself from adopting the same interference techniques used in the test rig | `spikes/SP-0-gui-harness/REPORT.md#4-rui-ro-moi-phat-hien` | resolved | Recorded as a product-wide prohibition in `req-002-gui-spike-harness`. |
| RISK-020 | Creating a task with a new option label permanently adds that option to the database schema; undo removes the value but not the orphaned option | `spikes/SP-1-notion-compensation/REPORT.md#4-rui-ro-moi-phat-hien` | open | Surface in the undo preview as a residue the compensation cannot remove. |
| RISK-021 | A status property cannot be set back to empty; undoing a creation leaves a default state rather than nothing | `spikes/SP-1-notion-compensation/REPORT.md#4-rui-ro-moi-phat-hien` | open | Specify the expected post-undo state so the preview does not promise an exact restoration. |
| RISK-022 | The platform sends no quota headers on successful responses, so the system learns it hit the limit only on rejection | `spikes/SP-1-notion-compensation/REPORT.md#4-rui-ro-moi-phat-hien` | mitigated | The client-side request queue is the only guard; specified at a 2.5 req/s ceiling in `req-003-notion-compensation`. |
| RISK-023 | Assigning a person to a task triggers an email notification the platform cannot recall | `spikes/SP-1-notion-compensation/REPORT.md#4-rui-ro-moi-phat-hien` | open | Declare as an out-of-band side effect in the manifest and show it in the undo preview. |
| RISK-024 | The cheap model agrees with the user rather than refusing, inventing rules for properties that do not exist | `spikes/SP-2-rule-elicitation/REPORT.md#4-rui-ro-moi-phat-hien` | mitigated | That model is forbidden in the elicitation role. See `req-004-rule-elicitation`. |
| RISK-025 | Everyday language has one word for delete where the platform API has two operations, so a rule blocking one leaves the other open | `spikes/SP-2-rule-elicitation/REPORT.md#4-rui-ro-moi-phat-hien` | mitigated | The specification-level definition of delete covers both operations. |
| RISK-026 | Blocking a page by identifier alone leaves its child pages and databases unprotected | `spikes/SP-2-rule-elicitation/REPORT.md#4-rui-ro-moi-phat-hien` | mitigated | The rule representation must support an ancestor check. Carried into `req-009-rule-ir-hardgate`. |
| RISK-027 | The designer cannot export binary animation files without a paid editor subscription | `spikes/SP-3-electron-rive/REPORT.md#4-rui-ro-moi-phat-hien` | open | Budget the subscription at the start of the animation design phase. |
| RISK-028 | The animation runtime deprecates the parameter the product would naturally use, warning of removal in a future major version | `spikes/SP-3-electron-rive/REPORT.md#4-rui-ro-moi-phat-hien` | mitigated | All product code drives the state machine through the frozen contract. See `req-005-electron-rive-pet-render`. |
| RISK-029 | The vision model prefers attached image content over the user's typed instruction when the two disagree | `spikes/SP-4-agent-loop/REPORT.md#4-rui-ro-moi-phat-hien` | mitigated | Source precedence specified: typed text outranks image content. See `req-006-agent-loop`. |
| RISK-030 | The agent sometimes asks its clarifying question as ordinary prose, which the user has no interface to answer | `spikes/SP-4-agent-loop/REPORT.md#4-rui-ro-moi-phat-hien` | mitigated | The system prompt mandates the ask tool; the scenario suite covers it as a regression case. |
| RISK-031 | The vision endpoint rejects images below 14 pixels in either dimension | `spikes/SP-6-pi-sdk/REPORT.md#4-rui-ro-moi-phat-hien` | mitigated | The composer validates image dimensions before sending. |
| RISK-032 | A similarly named package namespace exists; installing it breaks parallel-agent isolation and requires a different runtime | `spikes/SP-6-pi-sdk/REPORT.md#4-rui-ro-moi-phat-hien` | mitigated | Package identity and versions pinned in `req-007-pi-sdk-harness`. |
| RISK-033 | A newly created card window stalls the desktop compositor by roughly 30-50 ms, dropping one to two characters from a fast typist | `spikes/SP-7-pet-window-os/REPORT.md#4-rui-ro-moi-phat-hien` | mitigated | The card window is pre-warmed rather than created on demand. See `req-008-pet-window-os`. |
| RISK-034 | The test harness defect from the earlier spike was inherited here before being diagnosed | `spikes/SP-7-pet-window-os/REPORT.md#4-rui-ro-moi-phat-hien` | resolved | Removed; the prohibition is now product-wide. |
| RISK-035 | The native module adds a Rust toolchain and C++ build tools to every continuous integration runner | `spikes/SP-7-pet-window-os/REPORT.md#4-rui-ro-moi-phat-hien` | open | Provision the toolchain in the runner image; the requirement is unavoidable given the window behaviour needed. |
| RISK-036 | A blocked compound update can be split into separate single-field calls to evade the rule | `spikes/SP-8-rule-ir-hardgate/REPORT.md#4-rui-ro-moi-phat-hien` | mitigated | Rules constrain individual fields with pattern matching; verified against the adversarial corpus. |
| RISK-037 | The platform accepts the same property as a plain value or as a nested object, so a naive comparison misses a match | `spikes/SP-8-rule-ir-hardgate/REPORT.md#4-rui-ro-moi-phat-hien` | mitigated | The evaluator always normalises names and extracts nested values before comparing. |
| RISK-038 | The platform rounds its last-edited timestamp to the minute, so a third-party edit within the same minute is invisible to a timestamp check | `spikes/SP-9-undo-agent/REPORT.md#4-rui-ro-moi-phat-hien` | mitigated | Conflict detection compares property payloads, not timestamps, for every connector. See `req-010-undo-agent`. |
| RISK-039 | An internal integration token is refused on some endpoints that a production authorisation token would allow, so irreversibility differs between the two | `spikes/SP-9-undo-agent/REPORT.md#4-rui-ro-moi-phat-hien` | open | Re-confirm the irreversible set against production authorisation before the connector ships. |
| RISK-040 | Text impersonating internal metadata inside a title or description persuades the cheap model to downgrade its own control | `spikes/SP-10-risk-judge/REPORT.md#4-rui-ro-moi-phat-hien` | mitigated | Sensitive keywords are stripped or flagged before the judge call, which may escalate to the strong model or to the user. See `req-011-risk-judge`. |
| RISK-041 | Both models lean toward approving an ambiguous operation if it looks undoable, ignoring whose object it is | `spikes/SP-10-risk-judge/REPORT.md#4-rui-ro-moi-phat-hien` | mitigated | The static tier blocks operations on objects the user did not create, before the judge is consulted. |
| RISK-042 | An administrator resetting the operating system password out of band can desynchronise the encryption key, making stored credentials unreadable | `spikes/SP-11-secure-storage/REPORT.md#4-rui-ro-moi-phat-hien` | open | Decryption failure is a handled state that offers reconnection. See `req-012-secure-storage`. |
| RISK-043 | Running under a service account would leave no per-user encryption profile | `spikes/SP-11-secure-storage/REPORT.md#4-rui-ro-moi-phat-hien` | resolved | The product always runs in an interactive user session, never as a service. |
| RISK-044 | A tool with file-system write access could drop the database triggers that enforce append-only | `spikes/SP-12-sqlite-ledger/REPORT.md#4-rui-ro-moi-phat-hien` | open | Application-data directory permissions restricted to the application process; no network listener on the store. |
| RISK-045 | Crash handling for tools whose effect cannot be read back, such as sending a message | `spikes/SP-12-sqlite-ledger/REPORT.md#4-rui-ro-moi-phat-hien` | mitigated | Recovery moves the job to a user-confirmation state rather than retrying, so nothing is sent twice. |
| RISK-046 | An engine-internal binding in the older database library generation breaks on application framework upgrades | `spikes/SP-12-sqlite-ledger/REPORT.md#4-rui-ro-moi-phat-hien` | resolved | Pinned to the Node-API generation with prebuilt binaries; the older generation must never be reintroduced. |
| RISK-047 | A shell encoding default inserts a byte-order mark that breaks the native rebuild toolchain | `spikes/SP-12-sqlite-ledger/REPORT.md#4-rui-ro-moi-phat-hien` | open | Lint or pre-commit check for byte-order marks in configuration files. |
| RISK-048 | A test runner assuming POSIX signal semantics reports false results on Windows | `spikes/SP-12-sqlite-ledger/REPORT.md#4-rui-ro-moi-phat-hien` | mitigated | Cross-platform process-exit assertion helper in the test harness. |
| RISK-049 | Cross-connector deadlock when two jobs hold one resource each and wait on the other | `spikes/SP-15-concurrency/REPORT.md#4-rui-ro-moi-phat-hien` | mitigated | Canonical lock ordering by resource identifier, or acquiring all locks upfront. See `req-015-concurrency-coordinator`. |
| RISK-050 | A steady stream of interactive commands starves a long bulk job | `spikes/SP-15-concurrency/REPORT.md#4-rui-ro-moi-phat-hien` | mitigated | Weighted round-robin guarantees background jobs a minimum share of throughput. |
| RISK-051 | An individually issued certificate still triggers a reputation warning for the first beta users | `spikes/SP-16-signing-update/REPORT.md#4-rui-ro-moi-phat-hien` | open | Prepare user guidance and submit the build to the platform reputation service as soon as it is signed. |
| RISK-052 | A hardware-token certificate cannot be automated in continuous integration | `spikes/SP-16-signing-update/REPORT.md#4-rui-ro-moi-phat-hien` | mitigated | Cloud signing only; hardware-token packages excluded from procurement. |
| RISK-053 | Importing a cryptographic hardware token can be held at customs pending a licence, delaying release by up to a month | `spikes/SP-16-signing-update/REPORT.md#4-rui-ro-moi-phat-hien` | mitigated | Same mitigation: no hardware token is purchased. |
| RISK-054 | Sending an image to a model without vision returns an empty stream with no error, so the failure is invisible | `spikes/SP-17-provider-matrix/REPORT.md#4-rui-ro-moi-phat-hien` | mitigated | The composer checks declared input capability before sending and disables attachment when unavailable. |
| RISK-055 | Vision latency reaches 6.61 s median and 19.65 s at the ninetieth percentile, far beyond the acknowledgement commitment | `spikes/SP-17-provider-matrix/REPORT.md#4-rui-ro-moi-phat-hien` | mitigated | Immediate interface-level acknowledgement with the model response following asynchronously. See `req-017-provider-matrix`. |
| RISK-056 | Reading window titles collects personal information that would otherwise flow into the ledger and to a model | `spikes/SP-18-pet-liveness/REPORT.md#4-rui-ro-moi-phat-hien` | mitigated | Zero-persistent-title rule with filtering in the native layer; only category and geometry cross the boundary. See `req-018-pet-liveness`. |
| RISK-057 | Interfering with input-method processes conflicts with low-level system hooks and degrades the whole desktop | `spikes/SP-18-pet-liveness/REPORT.md#4-rui-ro-moi-phat-hien` | resolved | Permanently prohibited, including in test tooling. |
| RISK-058 | One platform offers no revocation endpoint, so disconnecting removes the local token while the integration stays authorised on the platform | `spikes/SP-19-connector-framework/REPORT.md#4-rui-ro-moi-phat-hien` | open | State this plainly in the disconnect flow and direct the user to the platform's own settings. See `req-019-connector-framework`. |
| RISK-059 | A token expiring mid-sequence costs an unexpected refresh round trip | `spikes/SP-19-connector-framework/REPORT.md#4-rui-ro-moi-phat-hien` | mitigated | The job manager checks expiry before starting and refreshes proactively. |
| RISK-060 | Two hundred concurrent writers pushed median database latency to about 1.5 s on a development machine | `spikes/SP-20-backend-slice/REPORT.md#4-rui-ro-moi-phat-hien` | open | Connection pooling in production, with read/write separation if scale exceeds the beta. |
| RISK-061 | During a long outage a user may queue commands that contradict each other | `spikes/SP-21-ask-user-offline/REPORT.md#4-rui-ro-moi-phat-hien` | mitigated | The app lets the user view, edit or cancel individual queued commands before they are sent. See `req-021-ask-user-offline`. |
| RISK-062 | A backend compromise now exposes user work content, which was structurally impossible before the amendment | `docs/spec/changes/req-022-account-sync/proposal.md` | open | Accepted deliberately: end-to-end encryption was rejected because recovery must work from account sign-in alone. The compensating controls are key access confined to the replication path, least privilege and audited access, which principle VII now states as the boundary. |
| RISK-063 | The key-access path widens over time from serving replication into support tooling, analytics or model input | `docs/spec/changes/req-022-account-sync/proposal.md` | open | Principle VII restricts backend use of account content to serving replication to that same account. Enforcement is review plus the audit record; no mechanism prevents a future feature from widening it. |
| RISK-064 | Account sign-in is now the sole gate on the user's entire history across all devices | `docs/spec/changes/req-022-account-sync/proposal.md` | open | Compromise of the identity account yields everything. Multi-factor authentication on the identity provider and device-registry visibility so the user can see and revoke unknown devices. |
| RISK-065 | Ledger replication resolved by last-writer-wins would silently delete history and break the append-only principle | `docs/spec/changes/req-022-account-sync/proposal.md` | mitigated | Ledger conflict resolution is specified as append-and-reconcile; last-writer-wins is forbidden for any append-only store. Specified in `req-022-account-sync`, not yet measured. |
| RISK-066 | A revoked or signed-out device retains a usable replicated connector token until it next contacts the server | `docs/spec/changes/req-022-account-sync/proposal.md` | partially mitigated | Revocation is enforced server-side so an offline device cannot be the only line of defence; the local wipe on sign-out is the second line. The window between revocation and the device noticing is unmeasured. |
| RISK-067 | User work content now rests on our infrastructure, which brings data-residency and legal obligations that did not previously apply | `docs/spec/changes/req-022-account-sync/proposal.md` | open | Escalates the open question on infrastructure provider and storage region from a deployment preference to a compliance decision. Tracked as `Q-OQ-11` in `req-001-mvp-product-definition`. |
| RISK-068 | Clock skew between devices reorders replicated ledger records and misleads undo or the readable history | `docs/spec/changes/req-022-account-sync/proposal.md` | open | Ordering must not rely on device wall-clock time; a per-device sequence with causal ordering is the likely answer but is unspecified and unmeasured. |
| RISK-069 | Server-side storage growth and retention are unmeasured, since the spike measured a single device's ledger | `spikes/SP-12-sqlite-ledger/REPORT.md#0-ket-luan` | open | The measured 12.4 MB at 1,800 jobs is per device; the account total is the union across devices, multiplied by the beta population. Needs sizing before the first beta. |
| RISK-070 | Replication has no spike behind it, so every property of the sync design is currently unverified | `docs/spec/changes/req-022-account-sync/proposal.md` | open | The twenty spikes measured a device-bound product. Conflict resolution, enrolment from sign-in alone, revocation latency and encrypted-store performance all need a spike before the design is trusted. |
| RISK-071 | AppleScript keystroke strips Vietnamese diacritics while CGEvent preserves them | `spikes/SP-0-gui-harness/macos/REPORT.md#4-rui-ro-moi-phat-hien` | resolved | Use CGEvent with CGEventKeyboardSetUnicodeString; avoid osascript keystroke for Unicode text. |
| RISK-072 | TCC permissions bound to DMG mount path require re-granting if the app binary is moved | `spikes/SP-0-gui-harness/macos/REPORT.md#4-rui-ro-moi-phat-hien` | open | Install app bundle to /Applications before running long automated test sessions. |
| RISK-073 | Screen lock and display sleep interrupt frame capture and background soak tests | `spikes/SP-0-gui-harness/macos/REPORT.md#4-rui-ro-moi-phat-hien` | resolved | Configure System Settings and run soak tests wrapped in caffeinate -dimsu. |
| RISK-074 | IPC latency of setIgnoreMouseEvents on plain Electron drops fast clicks on pet borders | `spikes/SP-7-pet-window-os/macos/REPORT.md#4-rui-ro-moi-phat-hien` | open | Implement AppKit native view hitTest: returning nil over transparent pixels for zero-latency kernel routing. |
| RISK-075 | Process-level NSApplicationActivationPolicy on macOS risks hiding management window from Dock/Switcher | `spikes/SP-7-pet-window-os/macos/REPORT.md#4-rui-ro-moi-phat-hien` | open | Dynamically switch activation policy between Accessory (pet only) and Regular (App Window opened). |
| RISK-076 | Notification banners on macOS are silenced or hidden when Focus / Do Not Disturb mode is active | `spikes/SP-7-pet-window-os/macos/REPORT.md#4-rui-ro-moi-phat-hien` | mitigated | Menu bar Tray icon acts as reliable persistent anchor for restoring hidden pet. |
| RISK-077 | Auto-update breaks Keychain ACL and prompts password unless signed with official Apple Developer ID | `spikes/SP-11-secure-storage/macos/REPORT.md#4-rui-ro-moi-phat-hien` | open | Mandate Apple Developer ID Application certificate for all macOS distribution builds before Closed Beta. |
| RISK-078 | Modifying application name changes Keychain service name and orphans existing master key | `spikes/SP-11-secure-storage/macos/REPORT.md#4-rui-ro-moi-phat-hien` | mitigated | Lock app.setName to 'DesktopAssistant' permanently across all versions. |
| RISK-079 | User accidental deletion of Keychain item breaks decryption of stored tokens | `spikes/SP-11-secure-storage/macos/REPORT.md#4-rui-ro-moi-phat-hien` | mitigated | Catch decryption errors and emit SYSTEM card prompting user to reconnect connector. |
| RISK-080 | @electron/universal fails on duplicate native prebuilds unless x64ArchFiles is declared | `spikes/SP-12-sqlite-ledger/macos/REPORT.md#4-rui-ro-moi-phat-hien` | mitigated | Declare `x64ArchFiles: "*.node"` in electron-builder mac configuration; prefer per-arch DMGs. |
| RISK-081 | Misapplying F_FULLFSYNC to transient scratchpad or log data severely degrades agent I/O throughput | `spikes/SP-12-sqlite-ledger/macos/REPORT.md#4-rui-ro-moi-phat-hien` | mitigated | Confine PRAGMA fullfsync to the authoritative Action Ledger and Job store alone. |
| RISK-082 | Chromium multi-process architecture on macOS consumes ~390 MB Working Set RAM when pet is idle | `spikes/SP-3-electron-rive/macos/REPORT.md#4-rui-ro-moi-phat-hien` | open | Implement idle animation rate reduction (30 fps or pause RAF on prolonged user inactivity >60s). |
| RISK-083 | Rive Web Runtime v2.42.1 deprecation warning for animations parameter | `spikes/SP-3-electron-rive/macos/REPORT.md#4-rui-ro-moi-phat-hien` | mitigated | Mandate stateMachine usage conforming to Contract v0. |
| RISK-084 | SecurityAgent prompts password on macOS Keychain access when binary is signed ad-hoc | `spikes/SP-22-macos-permissions/REPORT.md#4-rui-ro-moi-phat-hien` | open | Mandate Apple Developer ID Application certificate for distribution builds to ensure silent Keychain access. |
| RISK-085 | Invoking tccutil reset without bundle ID wipes system-wide TCC permissions and breaks agent harness | `spikes/SP-22-macos-permissions/REPORT.md#4-rui-ro-moi-phat-hien` | mitigated | Enforce mandatory bundle ID parameter in all testing scripts and documentation. |
| RISK-086 | Gatekeeper on macOS Sequoia (15+) eliminates Control-Click bypass for unnotarized apps | `spikes/SP-16-signing-update/macos/REPORT.md#4-rui-ro-moi-phat-hien` | open | Mandate Apple Developer ID Application signing and unmetered notarization before first closed beta. |
| RISK-087 | Cascading permission loss after auto-update if macOS binary is packaged with ad-hoc signature | `spikes/SP-16-signing-update/macos/REPORT.md#4-rui-ro-moi-phat-hien` | mitigated | Enforce designated requirement preservation via official Apple Developer ID or named keychain certificate in CI. |
| RISK-088 | Demanding Screen Recording permission for pet avatar triggers user backlash and monthly macOS 15+ warnings | `spikes/SP-18-pet-liveness/macos/REPORT.md#4-rui-ro-moi-phat-hien` | mitigated | Eliminate Screen Recording requirement for pet; rely only on Level 0 geometry (bounds, app name) and Level 1 Accessibility for caret. |
| RISK-089 | Fullscreen transparent overlay captures entire desktop and creates IPC event routing latency on mouse motion | `spikes/SP-18-pet-liveness/macos/REPORT.md#4-rui-ro-moi-phat-hien` | resolved | Reject Architecture B; adopt Architecture A (paired small floating windows) for zero-latency mouse routing. |

## Dropped sections

Sections read during harvest that carry no harvestable content, recorded here so `spec-check evidence` does not
keep asking for a citation and a later run does not re-read them. Each of these reports states that every spike
question was answered, so the "unanswered" section is empty by design.

- DROPPED spikes/SP-18-pet-liveness/macos/REPORT.md#5-chua-tra-loi-duoc-vi-sao
  Rationale: records hardware limitations on Mac mini (120Hz ProMotion, dual monitors with mixed scale, battery drain, notch, and Sidecar/AirPlay) tracked for dedicated laptop/multi-screen hardware.

- DROPPED spikes/SP-0-gui-harness/REPORT.md#5-chua-tra-loi-duoc-vi-sao
  Rationale: the report records no unanswered question for this spike, so there is nothing to place in a clarification queue.
- DROPPED spikes/SP-0-gui-harness/macos/REPORT.md#5-chua-tra-loi-duoc-vi-sao
  Rationale: the report records no unanswered question for this spike, so there is nothing to place in a clarification queue.
- DROPPED spikes/SP-7-pet-window-os/macos/REPORT.md#5-chua-tra-loi-duoc-vi-sao
  Rationale: records hardware limitations on Mac mini (mixed Retina/1x scaling and notch) tracked for dedicated hardware.
- DROPPED spikes/SP-11-secure-storage/macos/REPORT.md#5-chua-tra-loi-duoc-vi-sao
  Rationale: Time Machine cross-machine migration without account sync deferred for dedicated multi-machine hardware; canonical restore path is account sync from backend under Constitution Principle VII.
- DROPPED spikes/SP-12-sqlite-ledger/macos/REPORT.md#5-chua-tra-loi-duoc-vi-sao
  Rationale: all five questions answered by empirical code execution on Apple Silicon M1 (macOS 26.5.2, APFS FileVault).
- DROPPED spikes/SP-16-signing-update/macos/REPORT.md#5-chua-tra-loi-duoc-vi-sao
  Rationale: Apple Developer Program purchase lead time and notarization throughput timing deferred for PO procurement approval before M1 Closed Beta.
- DROPPED spikes/SP-3-electron-rive/macos/REPORT.md#5-chua-tra-loi-duoc-vi-sao
  Rationale: records hardware limitations on Mac mini (120Hz ProMotion display and battery drain rate) tracked for dedicated laptop hardware.
- DROPPED spikes/SP-4-agent-loop/REPORT.md#5-chua-tra-loi-duoc-vi-sao
  Rationale: the report records no unanswered question for this spike, so there is nothing to place in a clarification queue.
- DROPPED spikes/SP-6-pi-sdk/REPORT.md#5-chua-tra-loi-duoc-vi-sao
  Rationale: the report records no unanswered question for this spike, so there is nothing to place in a clarification queue.
- DROPPED spikes/SP-8-rule-ir-hardgate/REPORT.md#5-chua-tra-loi-duoc-vi-sao
  Rationale: the report records no unanswered question for this spike, so there is nothing to place in a clarification queue.
- DROPPED spikes/SP-9-undo-agent/REPORT.md#5-chua-tra-loi-duoc-vi-sao
  Rationale: the report records no unanswered question for this spike, so there is nothing to place in a clarification queue.
- DROPPED spikes/SP-10-risk-judge/REPORT.md#5-chua-tra-loi-duoc-vi-sao
  Rationale: the report records no unanswered question for this spike, so there is nothing to place in a clarification queue.
- DROPPED spikes/SP-12-sqlite-ledger/REPORT.md#5-chua-tra-loi-duoc-vi-sao
  Rationale: the report records no unanswered question for this spike, so there is nothing to place in a clarification queue.
- DROPPED spikes/SP-15-concurrency/REPORT.md#5-chua-tra-loi-duoc-vi-sao
  Rationale: the report records no unanswered question for this spike, so there is nothing to place in a clarification queue.
- DROPPED spikes/SP-17-provider-matrix/REPORT.md#5-chua-tra-loi-duoc-vi-sao
  Rationale: the report records no unanswered question for this spike, so there is nothing to place in a clarification queue.
- DROPPED spikes/SP-19-connector-framework/REPORT.md#5-chua-tra-loi-duoc-vi-sao
  Rationale: the report records no unanswered question for this spike, so there is nothing to place in a clarification queue.
- DROPPED spikes/SP-21-ask-user-offline/REPORT.md#5-chua-tra-loi-duoc-vi-sao
  Rationale: the report records no unanswered question for this spike, so there is nothing to place in a clarification queue.
- DROPPED spikes/SP-22-macos-permissions/REPORT.md#5-chua-tra-loi-duoc-vi-sao
  Rationale: system audio recording deferred to Phase 3 per roadmap §1; all seven MVP permission questions answered by empirical code execution.
## Dropped sources

Evaluated during harvest and deliberately not turned into changes, so a later run does not re-harvest them:

- DROPPED `docs/raw-idea/idea-brief.md` — superseded by PRD v2.1; its content is restated there, and harvesting both would duplicate every risk.
- DROPPED `docs/raw-idea/raw-idea.md` — the original brainstorm, superseded by the same document; it also carries no headings, so it cannot be cited by section.
- DROPPED `spikes/SP-*/README.md` — spike setup and protocol notes, not findings. The project's evidence configuration recognises `REPORT.md` only.
- DROPPED `spikes/SP-*/evidence/**` — raw logs, matrices and schemas cited by their own report; they are evidence for a change, not a change.
- DROPPED `spikes/fixtures/**` — frozen ground-truth corpora used to grade spikes, including the 40 elicitation transcripts. They are regression inputs, not proposals.
- DROPPED `spikes/secrets/README.md` — operational note on credential handling for spike runs.
